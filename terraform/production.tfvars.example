# Stage 4 scales the same fixed-size node group from one worker to two.
worker_count = 2

# The completed production rotation retains only the active green key.
application_s3_keys = {
  active = "green"
  slots  = ["green"]
}
